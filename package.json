{
  "name": "atm",
  "version": "0.0.1",
  "private": true,
  "description": "ATM - Application for Technical Management",
  "scripts": {
    "dev:backend": "cd backend && pnpm run start:dev",
    "dev:frontend": "cd frontend && pnpm run dev",
    "build:backend": "cd backend && pnpm run build",
    "build:frontend": "cd frontend && pnpm run build",
    "clean": "cd backend && rimraf dist",
    "build": "pnpm run clean && pnpm run build:backend && pnpm run build:frontend",
    "start:backend:prod": "cd backend && cross-env NODE_ENV=production pnpm run start:prod",
    "start:prod": "pnpm run build:backend && pnpm run build:frontend && pnpm run start:backend:prod",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "install:all": "pnpm install && cd backend && pnpm install && cd ../frontend && pnpm install"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "rimraf": "^6.1.2"
  }
}
