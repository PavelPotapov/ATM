# Frontend Environment Variables
# Copy this file to .env and fill in the values

# Backend API URL
# For local development (no ngrok): http://localhost:3000
# For ngrok tunnel (external access): https://your-ngrok-url.ngrok-free.app
# 
# IMPORTANT: This is the BACKEND URL, not frontend URL!
# - Use localhost:3000 if you're testing locally only
# - Use ngrok URL if you need external access (mobile, demo, etc.)
VITE_API_URL=http://localhost:3000